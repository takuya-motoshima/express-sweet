{
  "name": "demo",
  "version": "1.0.0",
  "scripts": {
    "start": "nodemon --ignore client/ --ignore public/ --ignore views/ bin/www",
    "//start": "pm2 start ecosystem.config.js",
    "//restart": "pm2 restart ecosystem.config.js",
    "//stop": "pm2 stop ecosystem.config.js"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "express-validator": "^6.14.2",
    "http-errors": "~1.6.3",
    "nodejs-shared": "^1.0.10"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
