<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{{block "title"}}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: "Inter", sans-serif;
      padding: 2rem 0;
    }
    .sidebar {
      position: sticky;
      top: 2rem;
    }
    .sidebar h5 {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 1rem;
    }
    .sidebar .nav-link {
      padding: 0.5rem 0.75rem;
      color: #495057;
      margin-bottom: 0.25rem;
      transition: all 0.2s;
      border-radius: 0.375rem;
      border-left: 3px solid transparent;
    }
    .sidebar .nav-link:hover {
      color: #212529;
      background-color: #f8f9fa;
    }
    .sidebar .nav-link.active {
      color: #0d6efd;
      font-weight: 600;
      background-color: #e7f1ff;
      border-left-color: #0d6efd;
    }
    .upload-section {
      padding: 0.5rem 0 1.5rem 0;
      margin-bottom: 1.5rem;
    }
    .upload-section h2 {
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    .upload-section p {
      color: #6c757d;
      font-size: 0.9rem;
      margin-bottom: 1rem;
    }
    .upload-section .alert-primary {
      background-color: transparent;
      border: none;
      color: #6c757d;
      padding: 0;
    }
    pre {
      background: #263238;
      color: #aed581;
      padding: 1rem;
      border-radius: 4px;
      overflow-x: auto;
      font-size: 0.8rem;
    }
    @media (max-width: 767.98px) {
      .sidebar-collapse {
        display: none;
      }
      .sidebar-collapse.show {
        display: block;
      }
    }
  </style>
  {{{block "style"}}}
</head>
<body>
  <main class="container">
    <div class="row">
      <div class="col-md-3">
        <div class="sidebar">
          <div class="d-flex justify-content-between align-items-center mb-3">
            <h5 class="mb-0">Upload Examples</h5>
            <button class="btn btn-primary d-md-none" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarNav">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 16 16">
                <path fill-rule="evenodd" d="M2.5 11.5A.5.5 0 0 1 3 11h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4A.5.5 0 0 1 3 7h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4A.5.5 0 0 1 3 3h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"></path>
              </svg>
            </button>
          </div>
          <nav class="nav flex-column sidebar-collapse collapse d-md-block" id="sidebarNav">
            <a class="nav-link {{#if (eq currentPath '/multipart-single-file')}}active{{/if}}" href="/multipart-single-file">Single File</a>
            <a class="nav-link {{#if (eq currentPath '/multipart-single-file-with-form')}}active{{/if}}" href="/multipart-single-file-with-form">Single File + Form</a>
            <a class="nav-link {{#if (eq currentPath '/multipart-multiple-files')}}active{{/if}}" href="/multipart-multiple-files">Multiple Files</a>
            <a class="nav-link {{#if (eq currentPath '/multipart-multiple-file-form')}}active{{/if}}" href="/multipart-multiple-file-form">Multiple File Form</a>
            <a class="nav-link {{#if (eq currentPath '/urlencoded-form')}}active{{/if}}" href="/urlencoded-form">URL-encoded Form</a>
            <a class="nav-link {{#if (eq currentPath '/json-file-with-form')}}active{{/if}}" href="/json-file-with-form">JSON File + Form</a>
          </nav>
        </div>
      </div>
      <div class="col-md-9">
        {{{body}}}
      </div>
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Auto-close sidebar on mobile when clicking a link
    document.querySelectorAll('#sidebarNav .nav-link').forEach(link => {
      link.addEventListener('click', () => {
        const sidebar = document.getElementById('sidebarNav');
        const bsCollapse = bootstrap.Collapse.getInstance(sidebar);
        if (bsCollapse && window.innerWidth < 768) {
          bsCollapse.hide();
        }
      });
    });
  </script>
  {{{block "script"}}}
</body>
</html>
